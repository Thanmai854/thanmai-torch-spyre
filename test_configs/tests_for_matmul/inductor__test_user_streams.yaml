test_suite_config:
  files:
  - path: ${PYTORCH_ROOT}/test/inductor/test_user_streams.py
    unlisted_test_mode: skip
    tests:
    - names:
      - TestUserStreamCompile::test_codegen_structure_parallel_matmuls
      mode: mandatory_success
      tags:
      - inductor
      edits:
        dtypes:
          include:
          - name: float32
        inputs:
          args:
          - tensor:
              shape:
              - 32
              - 32
              dtype: float16
              device: spyre
              init: rand
  global:
    devices:
    - spyre
    supported_dtypes:
    - name: float16
    - name: float32
    supported_ops: []
