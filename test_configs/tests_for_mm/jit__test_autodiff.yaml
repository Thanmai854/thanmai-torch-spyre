test_suite_config:
  files:
  - path: ${PYTORCH_ROOT}/test/jit/test_autodiff.py
    unlisted_test_mode: skip
    tests:
    - names:
      - TestAutodiffJit::test_requires_grad_outputs_profiled_twice
      mode: mandatory_success
      tags:
      - jit
      edits:
        dtypes:
          include:
          - name: float16
        inputs:
          args:
          - tensor:
              shape:
              - 10
              - 10
              dtype: float16
              device: spyre
              init: rand
  global:
    devices:
    - spyre
    supported_dtypes:
    - name: float16
    - name: float32
    supported_ops: []
