test_suite_config:
  files:
  - path: ${PYTORCH_ROOT}/test/inductor/test_inplacing_pass.py
    unlisted_test_mode: skip
    tests:
    - names:
      - TestReinplacingPassCorrectness::test_with_effects_reinplace_list_arg
      mode: mandatory_success
      tags:
      - inductor
      edits:
        dtypes:
          include:
          - name: float16
        inputs:
          args:
          - tensor:
              shape:
              - 3
              dtype: float16
              device: spyre
              init: rand
  global:
    devices:
    - spyre
    supported_dtypes:
    - name: float16
    - name: float32
    supported_ops: []
