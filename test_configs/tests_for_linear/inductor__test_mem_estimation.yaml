test_suite_config:
  files:
  - path: ${PYTORCH_ROOT}/test/inductor/test_mem_estimation.py
    unlisted_test_mode: skip
    tests:
    - names:
      - TestMemoryProfilingResNet::test_simple_linear_layers
      mode: mandatory_success
      tags:
      - inductor
      edits:
        dtypes:
          include:
          - name: float16
        inputs:
          args:
          - tensor:
              shape:
              - 10
              - 100
              dtype: float16
              device: spyre
              init: rand
  global:
    devices:
    - spyre
    supported_dtypes:
    - name: float16
    - name: float32
    supported_ops: []
