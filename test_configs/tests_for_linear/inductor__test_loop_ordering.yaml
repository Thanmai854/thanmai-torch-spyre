test_suite_config:
  files:
  - path: ${PYTORCH_ROOT}/test/inductor/test_loop_ordering.py
    unlisted_test_mode: skip
    tests:
    - names:
      - MemoryCoalescingTest::test_remapped_reads
      mode: mandatory_success
      tags:
      - inductor
      edits:
        dtypes:
          include:
          - name: float16
        inputs:
          args:
          - tensor:
              shape:
              - 4
              - 4
              dtype: float16
              device: spyre
              init: rand
    - names:
      - TestTiling::test_find_broadcast_var
      mode: mandatory_success
      tags:
      - inductor
      edits:
        dtypes:
          include:
          - name: float16
        inputs:
          args:
          - tensor:
              shape:
              - 1024
              - 256
              dtype: float16
              device: spyre
              init: rand
  global:
    devices:
    - spyre
    supported_dtypes:
    - name: float16
    - name: float32
    supported_ops: []
