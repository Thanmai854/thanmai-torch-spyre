test_suite_config:
  files:
  - path: ${PYTORCH_ROOT}/test/ao/sparsity/test_sparsity_utils.py
    unlisted_test_mode: skip
    tests:
    - names:
      - TestSparsityUtilFunctions::test_module_to_fqn_fail
      mode: mandatory_success
      tags:
      - ao
      edits:
        dtypes:
          include:
          - name: float16
        inputs:
          args:
          - tensor:
              shape:
              - 3
              - 3
              dtype: float16
              device: spyre
              init: rand
  global:
    devices:
    - spyre
    supported_dtypes:
    - name: float16
    - name: float32
    supported_ops: []
